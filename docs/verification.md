# 验证记录

环境：macOS 26.3.1 / arm64，Python 3.11.16，Node.js 25.9.0。

## 已运行

- `npm run build`：TypeScript 编译和 Vite 生产构建通过。
- `npm run test`：16 项通过。覆盖无损分段、完整分段合并、检查点复用、原图丢失时停止、原句歧义时拒绝补丁、关闭补丁、恢复版本、编辑冲突、路径穿越与符号链接限制、同名文件保护、URL 白名单、SRT/VTT/JSON 字幕解析、无字幕停止、任务重试、真实统计和密钥脱敏。
- 本机 HTTP 协议测试：启动隔离的模拟模型服务，使用实际 HTTP 请求验证 Chat Completions 和音频转写调用格式。
- MP4 集成检查：FFmpeg 生成一秒测试视频，再由实际音轨提取流水线转为 WAV，通过模拟语音接口生成字幕。
- Python 独立后端构建与 smoke test：验证冻结后可执行文件启动、SQLite 初始化、首页静态资源、设置读取和 API 鉴权；同时验证 Electron 内置 Node 支持 YouTube 解析所需的 permission 参数。
- 实际 Chrome 预览：首页成功渲染，真实零数据、导航、三个入口与空状态可见；已打开整理表单并输入 Markdown 验证按钮启用，也检查了视频链接表单及无字幕提示。
- Electron 更新到 44.4.1 后，安装时 npm 依赖审计报告 0 个已知漏洞。

- macOS Apple Silicon `.app` 打包成功，使用 ad-hoc 签名，体积约 434 MB；已实际独立启动，首页及设置页正常。
- 原生仓库选择窗口成功打开，并取消返回，保留用户已有仓库配置。
- 用户在桌面版配置文本模型后，已通过界面执行真实文本模型连接测试并显示成功；测试仅发送简短连接请求。

## 尚未声称完成的实测

- 尚未用真实长视频完成全文生成，也未调用真实语音转写服务。
- 已验证下方 issue 提供的 YouTube 视频字幕提取；尚未针对具体哔哩哔哩视频完成实测。
- Windows / Linux 分发与 Apple 公证未验证。

## 0.1.1：Issues #1–#4 修复验证（2026-09-17）

- #1：复现 `RlsOGvrpEsw` 的自动翻译中文字幕 HTTP 429。改为优先提取原始语言自动字幕，传递提取器请求头，原始字幕由模型生成中文笔记。修复后的 `fetch_subtitles` 实际取得 62,307 字符，视频时长 4,334 秒（约 72 分钟）；原始 JSON3 文件保留。未调用付费模型生成这段视频的完整笔记。
- 字幕回归覆盖：原语言优先于平台翻译、人工字幕语言偏好、哔哩哔哩内联 JSON、无效格式回退、429 明确提示且不反复请求、503 短暂失败重试。
- #2：实际 Chrome 预览确认移除「今天，想从哪里开始？」和「最近拾获」，「学习的足迹」横跨内容区。
- #3：临时数据库包含当前失败、历史失败和历史成功任务；进度面板只显示当前失败任务，点击「收起任务」后显示空状态，不显示旧历史。4 项前端筛选测试覆盖活动队列、当前失败、完成清空和重试。
- #4：使用隔离测试目录和虚构密钥，通过界面验证两处已保存密钥的显示/隐藏，保存后恢复隐藏；未查看或更改用户真实密钥。后端验证会话鉴权、来源限制、字段白名单、响应禁止缓存、日常读取脱敏和清除密钥。
- 自动化回归：`npm run test` 通过，4 项前端测试 + 22 项后端测试。
- `npm run package` 完成，产物版本为 0.1.1；冻结后端 smoke test 通过。已重新启动实际 `.app`，确认显示精简后的首页，并保留用户原有 Obsidian 仓库连接。
